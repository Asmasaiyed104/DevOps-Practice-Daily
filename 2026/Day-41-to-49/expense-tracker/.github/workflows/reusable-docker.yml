name: Reusable Docker Build, Push and Test

on:
  workflow_call:
    inputs:
      image_name:
        type: string
        required: true
      tag:
        type: string
        required: true
    secrets:
      docker_username:
        required: true
      docker_token:
        required: true
    outputs:
      image_url:
        description: "Full pushed image reference"
        value: ${{ jobs.docker.outputs.url }}

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.set_url.outputs.url }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t ${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }} .

      # Day 46 idea, adapted for this app: no HTTP server to curl, since
      # main.py is a CLI script that runs once and exits. Instead we run the
      # container and check that it exits 0 and prints the expected output.
      - name: Smoke-test the built image
        run: |
          output=$(docker run --rm ${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }})
          echo "$output"
          echo "$output" | grep -q "Total spent" || (echo "Expected output not found" && exit 1)

      # Day 49 - scan the image before it ever gets pushed.
      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }}
          format: table
          severity: 'CRITICAL,HIGH'
          exit-code: '1'

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.docker_username }}
          password: ${{ secrets.docker_token }}

      - name: Push Docker image
        run: docker push ${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }}

      - name: Set image URL output
        id: set_url
        run: echo "url=docker.io/${{ secrets.docker_username }}/${{ inputs.image_name }}:${{ inputs.tag }}" >> $GITHUB_OUTPUT
